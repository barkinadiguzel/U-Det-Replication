# U-Det-Replication
U-Det-Replication is a PyTorch implementation of U-Det for lung nodule segmentation, using a U-Net backbone with Bi-FPN for multi-scale feature fusion. It employs weighted binary cross-entropy and data augmentation to handle class imbalance and improve generalization.
